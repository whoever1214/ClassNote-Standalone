bin/
obj/
dist/
dist-v2/
publish/
*.user
*.suo
.vs/

# 测试产物（含备份数据库，禁止入库）
TestResults/*
# 例外：文档中被引用的测试 / 实测报告（纯文本，随仓库公开）
!TestResults/bench/
TestResults/bench/*
!TestResults/bench/performance-report.md
!TestResults/bench/v0.7-提速实测报告.md
!TestResults/bench/v0.7-主回路审计报告.md
!TestResults/reports/
TestResults/reports/*
!TestResults/reports/full-chain-test-report.md
*.log

# 分发包（体积大，不入库）
*.zip

# SenseVoice 模型二进制资产（约 230MB，非源码，不入库）
ClassNote/Models/sensevoice/

# UI 渲染工具的原始截图落盘目录（目录名由工作区路径拼接而来，属临时产物；
# 精选图已整理进 docs/ui-renders/，不入库）
*.ui-captures/

# 本地 NuGet 配置（含镜像源，按需自建）
# ClassNote/NuGet.config
